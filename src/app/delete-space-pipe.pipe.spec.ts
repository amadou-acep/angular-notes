import { DeleteSpacePipePipe } from './delete-space-pipe.pipe';

describe('DeleteSpacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteSpacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
