import { DeleteEtoilePipe } from './delete-etoile.pipe';

describe('DeleteEtoilePipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteEtoilePipe();
    expect(pipe).toBeTruthy();
  });
});
